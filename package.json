{
  "name": "foodtracker",
  "version": "1.2.0",
  "description": "Freundlicher, motivierender Foodtracker: Kalorien & Makros tracken, Barcode scannen, Ziel mit gesundem Tempo erreichen. Vanilla JS, läuft auf GitHub Pages.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test",
    "check": "node --check js/app.js && node --check js/calc.js && node --check js/api.js && node --check js/scanner.js && node --check js/storage.js && node --check js/db.js && node --check js/stats.js && node --check js/foods-data.js"
  },
  "license": "MIT",
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
